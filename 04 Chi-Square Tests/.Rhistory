setwd("C:/Users/John/Desktop/PSY 441/Capstone Reproducibility/04 Chi-Square Tests")
dat<-read.csv("homework4.csv")
```
```
dat<-read.csv("homework4.csv")
str(dat)
table(dat$control, dat$experimental)
chisq.test(dat$control,dat$experimental)
mytable
mytable
mytable<-table(dat$gender,dat$major)
mytable<-table(dat$control,dat$experimental)
table(dat$control, dat$experimental)
View(dat)
mytable(dat$control, dat$experimental)
mytable<-table(dat$agreement,dat$condition)
table(dat$agreement, dat$condition)
chisq.test(dat$agreement, dat$condition)
According the the results of the chi-test, the experiment didn't affect many of the people.
# Chunk 1
dat<-read.csv("homework4.csv")
# Chunk 2
str(dat)
# Chunk 3
table(dat$agreement, dat$condition)
